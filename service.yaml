apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  containers:
  - name: myapp-container
    image: nginx
   type: NodePort
   selector:
    app: MyApp
    ports:
     - port: 80
       targetPort: 80
       nodePort: 30008
